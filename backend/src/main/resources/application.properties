# Database Configuration (allow override via env; default to local MySQL)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/fastfood_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:123456}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration (allow override via env; default provided)
app.jwt.secret=${JWT_SECRET:ThisIsASecretKeyForJwtHs512ThisIsASecretKeyForJwtHs512}
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# WebSocket Configuration (allow override via env)
spring.websocket.path=${WS_PATH:/ws}

# VNPay Configuration (allow override via env)
vnPay.version=${VNPAY_VERSION:2.1.0}
vnPay.command=${VNPAY_COMMAND:pay}
vnPay.orderType=${VNPAY_ORDER_TYPE:other}
vnPay.currCode=${VNPAY_CURR_CODE:VND}
vnPay.hash-secret=${VNPAY_HASH_SECRET:ANAIDKYHLTU5HVKRMUM4NVRMLCN0SJU1}
vnPay.tmn-code=${VNPAY_TMN_CODE:8VWZT7KJ}
vnPay.payment-url=${VNPAY_PAYMENT_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnPay.return-url=${VNPAY_RETURN_URL:http://localhost:3000/checkout/payment-callback}
vnPay.demo=${VNPAY_DEMO:false}
vnPay.timezone=${VNPAY_TIMEZONE:GMT+7}

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Swagger UI config (springdoc)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui
springdoc.swagger-ui.operationsSorter=alpha
springdoc.swagger-ui.tagsSorter=alpha

# Logging Configuration
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR
logging.level.com.fastfood=DEBUG
logging.level.org.springframework.security=DEBUG

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Upload directory configuration (relative to project root)
app.upload-dir=uploads
app.images.dir=uploads/products
app.public-base=

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dfrkjvmym}
cloudinary.api-key=${CLOUDINARY_API_KEY:786228823246937}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:NhekeYfnbhHjQiJvlEHJr4KC3f8}
cloudinary.secure=true

# Map Configuration (allow override via env)
map.api.key=${MAP_API_KEY:}


