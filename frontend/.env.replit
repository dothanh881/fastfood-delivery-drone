# Frontend Replit deployment environment
# Use this file as a reference for Replit Secrets or for production builds in Replit.
# CRA reads REACT_APP_* variables at build time.

# Backend API base URL (include /api because backend sets context-path=/api)
REACT_APP_API_BASE_URL=https://<backend-repl>.replit.dev/api